# PR 1 worker spec: numerical contract, inventory, regression harness

Branch: `paydaycore/pr1-contract-fixtures` from `paydaycore/pr0-ci-package` (or szakacsmedia once PR 0 merges).
Plan: /Users/tyler/.claude/plans/option-b-b-full-effervescent-kite.md, sections "Metric registry" and "PR 1".

## Deliverables
1. `docs/METRICS.md`: the metric registry table from the plan + the consumer inventory (every screen, intent, widget face, notification, export, API endpoint, with its CURRENT formula and helper chain). Source for the inventory: the 2026-09-17 exploration (main session will paste it into the worker prompt).
2. Package types, no engine yet: `MetricID` (enum with every registry row), `EarningsComponents` (AdditiveArithmetic; cash, credit, gratuityFees, tipOut, regularWages, overtimeWages; computed nonWage/wages/earnedIncome), `Completeness` (`.off | .complete | .estimated | .partial(missingHours:missingRate:) | .noShifts` + counts), `EarningsResult` shell, `InputManifest` (SHA-256 via CryptoKit over a canonical sorted encoding; sub-digests shifts/paychecks/schedule/policies; fixed expected hex in a test), `CivilDay`, `DayRange`, `YearMonth`.
3. Fixtures `Packages/PaydayCore/Tests/PaydayCoreTests/Fixtures/*.json`, each `{ id, description, inputs, expected }`, loaded by a `FixtureLoader`. Expected values (all verified 2026-09-17):

| ID | Inputs | Expected |
|---|---|---|
| W1 | shifts 255 min and 330 min, same Mon-start week, rate 283c | per-shift regular 1203, 1556; week 2759; NOT 2760 |
| W2 | Mon Sep 28 2026 615m, Tue 585m, Wed 630m, Thu Oct 1 690m, Fri Oct 2 360m; rate 283c; Mon-start; 40h/1.5x | regular 2901/2759/2972/2688/0 = 11320; OT 0/0/0/849/2547 = 3396; Sep = 8632; Oct = 6084; total 14716; month-first wrong answer 13585 |
| W3 | W2 inputs with calendar-grid firstWeekday flipped Sunday<->Monday, payroll workweekStart unchanged | identical valuations |
| N1 | legacy TipEntry pair cash 6000 + credit 4000, tipOut 1000 on BOTH rows | migrated ShiftRecord tipOut 1000; nonWage 9000 (not 8000) |
| N2 | legacy pair with the same v1 receipt payload (gratuity 500, amount includes it) on both rows | gratuity counted once; voluntary normalized once |
| N3 | legacy nil-shiftID cash 3000 + credit 5000 same day | one ShiftRecord, cash 3000, credit 5000 |
| M1 | any W2 day | chart point cents == day earnedIncome |
| H1 | shift A 10000c 300 min, shift B 10000c no hours, no wage policy | hourlyRate 2000c/h (not 4000); coverage 1 of 2 |
| P1 | paycheck paidTips 10000, regular 5000, OT 0, gratuity 0, gross 15050 | observed 10000, proposed correction 10050, observed unchanged |
| E1 | hours 383/60 | CSV hours "6.3833" and "6:23"; never "6.5" |
| S2 | W2 plus a shift dated Oct 9 (future) with asOf Oct 2 | excluded from tips AND wages |
| Z1 | shift 300 min, cash 0, credit 0, rate 283c | ShiftRecord persists; regular 1415 |
| T1 | W2 evaluated with device TZ America/New_York then Pacific/Honolulu, policy TZ fixed America/New_York | identical valuations |
| C1 | W2 with Thu hours removed | headline uses knownComponents; completeness .partial(missingHours: 1); label "Known so far" |

4. Tag every fixture test that cannot pass yet with `.tags(.knownIssue)`; add `KnownIssuesGateTests.knownIssueCountIsZero` (reads a manifest of tagged tests) excluded from the per-PR CI run, required by the release workflow (PR 8 wires it).
5. Update `docs/PRODUCT.md` Pillar 8 with a "Known divergences under repair" list referencing fixture IDs.

Gate: `swift test` green (with knownIssue tests reported as known); every inventoried number has a MetricID; each audit finding maps to a fixture ID.
